# Daily Report - 2026-04-17

## Executive Summary

Today’s combined work moved the Illumios operating stack into a clearer pre-launch state:

- `admin.illumios.com` is live on Vercel using the exact working static dashboard
- the website code now supports a founding-cohort / priority-list waitlist flow instead of a stalled application-paused state
- the student portal direction is now locked around `Illumios Academia Hub` and is ready for implementation planning
- `Prospecting Website Builder` now has a working queue-first MVP prototype instead of just a product idea

## Completed Without Redundancy

### Admin

- Reset the admin migration back to the exact existing dashboard experience instead of a replacement shell
- Added the static Vercel build/run path for the current admin app
- Deployed the exact admin replica to Vercel
- Cut over `admin.illumios.com`
- Archived the abandoned Next.js rebuild path for reference instead of leaving it as the active runtime

### Website

- Tightened the site design and messaging so it feels more polished and conversion-ready
- Reframed the website from `applications paused` to a `founding cohort / priority list` launch mode
- Updated the current Next.js flow to collect `first name + email` for the waitlist instead of redirecting straight into the quiz
- Updated the API / lead-processing path so waitlist submissions can persist as `waitlist` leads and send a waitlist-specific payload to GHL
- Verified the updated code with lint and build

### Student Portal / Hub

- Created the initial live-class / portal PRD
- Aligned the repo to the real planned product: `Illumios Academia Hub`
- Locked the intended domain and direction around `hub.illumios.com`
- Locked the recommended stack:
  - `Next.js`
  - `React`
  - `TypeScript`
  - `Supabase`
  - `Vercel`
  - `OpenAI`
- Identified `Zoom Webinar` as the recommended MVP live delivery layer, with `Google Meet` as a constrained pilot option

### Prospecting Website Builder

- Ran product review and locked the approved MVP shape
- Documented the design and decision log
- Scaffolded a new Next.js app
- Built the queue-first review screen and prospect detail pages
- Added seeded domain models and reusable review UI components
- Verified the prototype with lint, build, and local page checks

## Key Decisions

- The admin dashboard remains a lightweight static app, not a Next.js rebuild
- The website should act like a pre-launch founding-cohort front door until live handoffs are fully wired
- The Hub should follow the `illumios-website` app-stack pattern, not the static admin architecture
- The first Hub implementation move should be an implementation plan, then a scaffold
- The Prospecting Website Builder is now a real seeded MVP prototype, but still not a live operating system

## Active Blockers

- The website waitlist flow still needs a real GHL workflow + inbound webhook URL
- The Vercel project for the website still needs environment variables for the live waitlist handoff
- The Hub still needs an implementation plan, route map, schema, and app scaffold
- The Prospecting Website Builder still needs a persistence decision, source-ingestion path, and production/deployment decision

## Recommended Next Sequence

1. Create the website GHL waitlist workflow and capture the inbound webhook URL
2. Add the website Vercel environment variables and deploy the waitlist version live
3. Turn the Hub PRD into an implementation plan
4. Scaffold the Hub app on `Next.js + TypeScript + Supabase + Vercel`
5. Decide the next real build slice for `Prospecting Website Builder`:
   - persistence layer
   - ingestion path
   - AI qualification service interface
   - deployment target
